const versions = {
  0: groupIds => {
    const request = require('kafkajs/src/protocol/requests/deleteGroups/v0/request')
    const response = require('kafkajs/src/protocol/requests/deleteGroups/v0/response')
    return { request: request(groupIds), response }
  },
  1: groupIds => {
    const request = require('kafkajs/src/protocol/requests/deleteGroups/v1/request')
    const response = require('kafkajs/src/protocol/requests/deleteGroups/v1/response')
    return { request: request(groupIds), response }
  },
}

module.exports = {
  versions: Object.keys(versions),
  protocol: ({ version }) => versions[version],
}
