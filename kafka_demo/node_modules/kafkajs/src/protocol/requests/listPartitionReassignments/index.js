const versions = {
  0: ({ topics, timeout }) => {
    const request = require('kafkajs/src/protocol/requests/listPartitionReassignments/v0/request')
    const response = require('kafkajs/src/protocol/requests/listPartitionReassignments/v0/response')
    return { request: request({ topics, timeout }), response }
  },
}

module.exports = {
  versions: Object.keys(versions),
  protocol: ({ version }) => versions[version],
}
